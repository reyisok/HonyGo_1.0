# HonyGo - 开发和测试依赖
# 用于开发环境的额外依赖包

# 包含生产依赖
-r requirements.txt

# 开发和测试工具
pytest>=7.4.0
pytest-qt>=4.2.0

# 配置文件处理（开发用）
pyyaml>=6.0

# GPU监控（开发调试用）
nvidia-ml-py>=12.535.0

# 深度学习视觉（开发测试用）
torchvision>=0.15.0

# 注意：以下依赖已移除（未在代码中使用）：
# - coloredlogs (已在生产依赖中移除)
# - pynput (已在生产依赖中移除)